{
  "buildCommand": "cd apps/web && mkdir -p prisma && cp ../../packages/database/prisma/schema.prisma ./prisma/schema.prisma && npx prisma generate && npx next build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
